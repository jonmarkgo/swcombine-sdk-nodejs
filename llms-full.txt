# swcombine-sdk v3.2.4

> Comprehensive TypeScript SDK for Star Wars Combine API v2.0

## Installation

```bash
npm install swcombine-sdk
```

---

## Client

# Class: SWCombine

Main SW Combine SDK client

## See

https://www.swcombine.com/ws/v2.0/developers/index.php SW Combine API Documentation

## Constructors

### Constructor

> **new SWCombine**(`config?`): `SWCombine`

#### Parameters

##### config?

`ClientConfig` = `{}`

#### Returns

`SWCombine`

## Properties

### api

> `readonly` **api**: `ApiResource`

***

### auth

> `readonly` **auth**: `object`

#### getAuthorizationUrl()

> **getAuthorizationUrl**: (`options`) => `string`

##### Parameters

###### options

`OAuthAuthorizationOptions`

##### Returns

`string`

#### handleCallback()

> **handleCallback**: (`query`) => `Promise`\<`AuthorizationResult`\>

##### Parameters

###### query

`OAuthCallbackQuery`

##### Returns

`Promise`\<`AuthorizationResult`\>

#### revokeToken()

> **revokeToken**: (`refreshToken`) => `Promise`\<`void`\>

##### Parameters

###### refreshToken

`string`

##### Returns

`Promise`\<`void`\>

***

### character

> `readonly` **character**: `CharacterResource`

***

### datacard

> `readonly` **datacard**: `DatacardResource`

***

### events

> `readonly` **events**: `EventsResource`

***

### faction

> `readonly` **faction**: `FactionResource`

***

### galaxy

> `readonly` **galaxy**: `GalaxyResource`

***

### inventory

> `readonly` **inventory**: `InventoryResource`

***

### location

> `readonly` **location**: `LocationResource`

***

### market

> `readonly` **market**: `MarketResource`

***

### news

> `readonly` **news**: `NewsResource`

***

### types

> `readonly` **types**: `TypesResource`

## Methods

### clearToken()

> **clearToken**(): `void`

Clear the token

#### Returns

`void`

***

### getRateLimitInfo()

> **getRateLimitInfo**(): `RateLimitInfo` \| `null`

Get the current rate limit information.
Returns the last known rate limit info from API response headers.
Returns null if no API calls have been made yet.

Note: The SW Combine API has a default limit of 600 requests per hour.

#### Returns

`RateLimitInfo` \| `null`

#### Example

```typescript
const rateLimit = client.getRateLimitInfo();
if (rateLimit) {
  console.log(`${rateLimit.remaining}/${rateLimit.limit} requests remaining`);
  console.log(`Resets at: ${rateLimit.resetTime}`);
}
```

***

### getToken()

> **getToken**(): `OAuthToken` \| `null`

Get the current token

#### Returns

`OAuthToken` \| `null`

***

### hasRefreshToken()

> **hasRefreshToken**(): `boolean`

Check if refresh token is available

#### Returns

`boolean`

***

### isTokenExpired()

> **isTokenExpired**(): `boolean`

Check if token is expired

#### Returns

`boolean`

***

### onRateLimitUpdate()

> **onRateLimitUpdate**(`callback`): `void`

Set a callback to be notified when rate limit info is updated after each API call.
Useful for monitoring rate limit consumption in real-time.

#### Parameters

##### callback

(`info`) => `void`

#### Returns

`void`

#### Example

```typescript
client.onRateLimitUpdate((info) => {
  if (info.remaining < 100) {
    console.warn(`Warning: Only ${info.remaining} API requests remaining!`);
  }
});
```

***

### refreshToken()

> **refreshToken**(): `Promise`\<`void`\>

Manually refresh the token

#### Returns

`Promise`\<`void`\>

***

### setToken()

> **setToken**(`token`): `void`

Set the access token

#### Parameters

##### token

`string` | `OAuthToken`

#### Returns

`void`

---

## Api

# Class: ApiResource

API resource for utility endpoints

## See

https://www.swcombine.com/ws/v2.0/documentation/api/helloworld/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new ApiResource**(`http`): `ApiResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`ApiResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### getResources()

> **getResources**(): `Promise`\<`ResourceInfo`[]\>

Get list of available API resources

#### Returns

`Promise`\<`ResourceInfo`[]\>

List of available API resources

#### Example

```ts
const resources = await client.api.getResources();
```

***

### helloAuth()

> **helloAuth**(): `Promise`\<`HelloAuthResponse`\>

Print a HelloWorld message for authorized clients
@requires_auth Yes
@requires_scope Any valid scope (used for testing authentication)

#### Returns

`Promise`\<`HelloAuthResponse`\>

#### Example

```ts
const response = await client.api.helloAuth();
```

***

### helloWorld()

> **helloWorld**(): `Promise`\<`string`\>

Print a HelloWorld message
@requires_auth No

#### Returns

`Promise`\<`string`\>

Plain string "Hello World"

#### Example

```ts
const message = await client.api.helloWorld();
console.log(message); // "Hello World"
```

***

### permissions()

> **permissions**(): `Promise`\<`Permission`[]\>

Get list of available web services permissions
@requires_auth No

#### Returns

`Promise`\<`Permission`[]\>

#### Example

```ts
const permissions = await client.api.permissions();
permissions.forEach(p => console.log(p.attributes.name, p.attributes.description));
```

***

### rateLimits()

> **rateLimits**(): `Promise`\<`RateLimitEntry`[]\>

Get current rate limit status for all endpoint patterns
@requires_auth No (shows public IP rate limits)

#### Returns

`Promise`\<`RateLimitEntry`[]\>

Array of rate limit entries, one per endpoint pattern

#### Example

```ts
const rateLimits = await client.api.rateLimits();
rateLimits.forEach(rl => {
  console.log(`${rl.attributes.pattern}: ${rl.attributes.remaining}/${rl.attributes.limit}`);
});
```

***

### time()

> **time**(`options?`): `Promise`\<`TimeResponse`\>

Get current time or convert times

#### Parameters

##### options?

Optional conversion parameters

###### cgt?

`string`

CGT format time to convert

###### time?

`number`

Unix timestamp to convert

#### Returns

`Promise`\<`TimeResponse`\>

#### Example

```ts
// Get current time
const current = await client.api.time();
// Convert CGT to unix
const converted = await client.api.time({ cgt: '2023-12-01 12:00:00' });
// Convert unix to CGT
const converted = await client.api.time({ time: 1701432000 });
```

---

## Character

# Class: CharacterCreditlogResource

Character credit log resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/creditlog/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterCreditlogResource**(`http`): `CharacterCreditlogResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterCreditlogResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`Page`\<`CreditLogEntry`\>\>

Get character's credit log (paginated)

@requires_auth Yes
@requires_scope CHARACTER_CREDITS

#### Parameters

##### options

`GetCharacterCreditlogOptions`

Character UID and optional pagination/filtering parameters

#### Returns

`Promise`\<`Page`\<`CreditLogEntry`\>\>

#### Example

```ts
const creditlog = await client.character.creditlog.list({ uid: '1:12345' });
const moreLogs = await client.character.creditlog.list({ uid: '1:12345', start_index: 51, item_count: 100 });
const oldestLogs = await client.character.creditlog.list({ uid: '1:12345', start_id: 1 });
// Fetch up to 1000 credit log entries at once
const manyLogs = await client.character.creditlog.list({ uid: '1:12345', item_count: 1000 });

// Iterate through all pages automatically
for await (const entry of creditlog) {
  console.log(entry);
}
```

# Class: CharacterCreditsResource

Character credits resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/credits/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterCreditsResource**(`http`): `CharacterCreditsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterCreditsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`number`\>

Get character's credit balance
@requires_auth Yes
@requires_scope CHARACTER_CREDITS

#### Parameters

##### options

`GetCharacterCreditsOptions`

#### Returns

`Promise`\<`number`\>

Credit balance as a plain number

#### Example

```ts
const credits = await client.character.credits.get({ uid: '1:12345' });
console.log(`Credits: ${credits}`); // Credits: 320089347
```

***

### transfer()

> **transfer**(`options`): `Promise`\<`unknown`\>

Transfer credits from character to another character or faction

#### Parameters

##### options

###### amount

`number`

Amount to transfer

###### reason?

`string`

Reason for transfer (optional, API will auto-append client name)
@requires_auth Yes
@requires_scope CHARACTER_CREDITS_WRITE

###### recipient

`string`

Recipient character or faction name/UID (REQUIRED)

###### uid

`string`

Character UID

#### Returns

`Promise`\<`unknown`\>

# Class: CharacterMessagesResource

Character messages resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/messages/mode/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterMessagesResource**(`http`): `CharacterMessagesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterMessagesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### create()

> **create**(`options`): `Promise`\<`Message`\>

Send a message
@requires_auth Yes
@requires_scope MESSAGES_SEND

#### Parameters

##### options

`CreateMessageOptions`

#### Returns

`Promise`\<`Message`\>

Message response typed as `Message`

#### Examples

```ts
// Valid: handles
await client.character.messages.create({
  uid: '1:12345',
  receivers: 'recipient_handle_1;recipient_handle_2',
  communication: 'Hello from the SDK!'
});
```

```ts
// Invalid: UIDs are rejected by the API and pre-validated by this SDK
await client.character.messages.create({
  uid: '1:12345',
  receivers: '1:12345',
  communication: 'Test'
});
```

***

### delete()

> **delete**(`options`): `Promise`\<`void`\>

Delete a message
@requires_auth Yes
@requires_scope MESSAGES_DELETE

#### Parameters

##### options

`DeleteMessageOptions`

#### Returns

`Promise`\<`void`\>

***

### get()

> **get**(`options`): `Promise`\<`Message`\>

Get a specific message
@requires_auth Yes
@requires_scope MESSAGES_READ

#### Parameters

##### options

`GetMessageOptions`

Character UID and message UID

#### Returns

`Promise`\<`Message`\>

Full message details including `communication`

#### Example

```ts
import { MessageMode } from 'swcombine-sdk';

const messages = await client.character.messages.list({ uid: '1:12345', mode: MessageMode.Received });
const messageId = messages.data[0]?.attributes.uid;

if (messageId) {
  const message = await client.character.messages.get({ uid: '1:12345', messageId });
  console.log(message.communication);
}
```

***

### list()

> **list**(`options`): `Promise`\<`Page`\<`MessageListItem`\>\>

List messages sent or received by character (paginated)

Note: list responses return `MessageListItem[]` metadata objects and do not
guarantee a `communication` field. Use `get()` for full message content.

@requires_auth Yes
@requires_scope MESSAGES_READ

#### Parameters

##### options

`ListMessagesOptions`

Character UID, optional message mode, and optional pagination parameters

#### Returns

`Promise`\<`Page`\<`MessageListItem`\>\>

Page of message metadata items (`Page<MessageListItem>`)

#### Example

```ts
import { MessageMode } from 'swcombine-sdk';

const allMessages = await client.character.messages.list({ uid: '1:12345' });
const received = await client.character.messages.list({ uid: '1:12345', mode: MessageMode.Received });
const moreMessages = await client.character.messages.list({ uid: '1:12345', mode: MessageMode.Received, start_index: 51, item_count: 50 });

const firstMessageId = received.data[0]?.attributes.uid;
if (firstMessageId) {
  const detail = await client.character.messages.get({ uid: '1:12345', messageId: firstMessageId });
  console.log(detail.communication);
}

// Iterate through all pages automatically
for await (const msg of allMessages) {
  console.log(msg.attributes.uid);
}
```

# Class: CharacterPermissionsResource

Character permissions resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/permissions/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterPermissionsResource**(`http`): `CharacterPermissionsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterPermissionsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### getScopes()

> **getScopes**(`options`): `Promise`\<`string`[]\>

Get flat list of granted scope names

#### Parameters

##### options

`GetCharacterPermissionsOptions`

#### Returns

`Promise`\<`string`[]\>

Array of scope strings (e.g., ['CHARACTER_READ', 'MESSAGES_SEND'])

***

### list()

> **list**(`options`): `Promise`\<`CharacterPermissionsResponse`\>

Get permissions granted to API client for this character

#### Parameters

##### options

`GetCharacterPermissionsOptions`

#### Returns

`Promise`\<`CharacterPermissionsResponse`\>

Full permissions response with scopes and expiration

#### Example

```ts
const perms = await client.character.permissions.list({ uid: '1:12345' });
const scopes = perms.permission[0]?.scopes.scope || [];
console.log('Granted scopes:', scopes.join(', '));
```

# Class: CharacterPrivilegesResource

Character privileges resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/privileges/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterPrivilegesResource**(`http`): `CharacterPrivilegesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterPrivilegesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`PrivilegeDetail`\>

Get a specific privilege.

Returns the `PrivilegeDetail` object directly — not wrapped in a `Page`.

@requires_auth Yes
@requires_scope CHARACTER_PRIVILEGES

#### Parameters

##### options

###### faction_id?

`number`

Optional faction ID to view privileges for (defaults to token owner's primary faction)

###### privilege

`string`

Privilege name

###### privilegeGroup

`string`

Privilege group name

###### uid

`string`

Character UID

#### Returns

`Promise`\<`PrivilegeDetail`\>

The `PrivilegeDetail` entity.

***

### list()

> **list**(`options`): `Promise`\<`PrivilegesResponse`\>

Get character's privileges organized by group
@requires_auth Yes
@requires_scope CHARACTER_PRIVILEGES

#### Parameters

##### options

`GetCharacterPrivilegesOptions` & `object`

#### Returns

`Promise`\<`PrivilegesResponse`\>

Privileges response with groups and metadata

#### Example

```ts
const privs = await client.character.privileges.list({ uid: '1:12345' });
privs.privilegegroup.forEach(g => {
  console.log(`${g.attributes.name}: ${g.attributes.count} privileges`);
});
```

***

### update()

> **update**(`options`): `Promise`\<`unknown`\>

Update a specific privilege (grant or revoke)

#### Parameters

##### options

###### faction_id?

`number`

Optional faction ID to view privileges for (defaults to token owner's primary faction)

###### privilege

`string`

Privilege name

###### privilegeGroup

`string`

Privilege group name

###### revoke?

`boolean`

Set to true to revoke the privilege, false/undefined to grant it

###### uid

`string`

Character UID

#### Returns

`Promise`\<`unknown`\>

# Class: CharacterResource

Character resource for managing characters

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterResource**(`http`): `CharacterResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### creditlog

> `readonly` **creditlog**: `CharacterCreditlogResource`

***

### credits

> `readonly` **credits**: `CharacterCreditsResource`

***

### messages

> `readonly` **messages**: `CharacterMessagesResource`

***

### permissions

> `readonly` **permissions**: `CharacterPermissionsResource`

***

### privileges

> `readonly` **privileges**: `CharacterPrivilegesResource`

***

### skills

> `readonly` **skills**: `CharacterSkillsResource`

## Methods

### get()

> **get**(`options`): `Promise`\<`Character`\>

Get character by UID.

Returns the `Character` object directly — not wrapped in a `Page`.

@requires_auth Yes
@requires_scope CHARACTER_READ

#### Parameters

##### options

`GetCharacterOptions`

Character UID

#### Returns

`Promise`\<`Character`\>

The `Character` entity.

#### Example

```ts
const character = await client.character.get({ uid: '1:12345' });
console.log(character.name); // access properties directly, not character.data
```

***

### getByHandle()

> **getByHandle**(`options`): `Promise`\<\{ `handle`: `string`; `uid`: `string`; \}\>

Look up a character's UID by their handle (username).

This endpoint is public — no authentication required — and is the primary
way to resolve a handle to the `uid` used by every other character endpoint.

#### Parameters

##### options

`GetCharacterByHandleOptions`

#### Returns

`Promise`\<\{ `handle`: `string`; `uid`: `string`; \}\>

`{ uid, handle }` — both fields are always present

#### Example

```ts
const { uid, handle } = await client.character.getByHandle({ handle: 'Dreks Selmur' });
console.log(uid);    // "1:46931"
console.log(handle); // "Dreks Selmur"

// Chain into a full profile fetch (requires CHARACTER_READ)
const character = await client.character.get({ uid });
```

***

### hasPermission()

> **hasPermission**(`options`): `Promise`\<`boolean`\>

Check if the API client has a specific permission for this character

#### Parameters

##### options

###### permission

`string`

Permission scope to check (e.g., 'CHARACTER_READ', 'MESSAGES_SEND')

###### uid

`string`

Character UID

#### Returns

`Promise`\<`boolean`\>

True if the permission is granted, false otherwise

#### Example

```ts
const canRead = await client.character.hasPermission({ uid: '1:12345', permission: 'CHARACTER_READ' });
if (canRead) {
  const character = await client.character.get({ uid: '1:12345' });
}
```

***

### me()

> **me**(): `Promise`\<`CharacterMe`\>

Get the currently authenticated user's character
@requires_auth Yes
@requires_scope CHARACTER_READ

#### Returns

`Promise`\<`CharacterMe`\>

The authenticated character's full profile

#### Example

```ts
const myCharacter = await client.character.me();
```

# Class: CharacterSkillsResource

Character skills resource

## See

https://www.swcombine.com/ws/v2.0/documentation/character/uid/skills/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new CharacterSkillsResource**(`http`): `CharacterSkillsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`CharacterSkillsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`CharacterSkills`\>

Get character's skills organized by category
@requires_auth Yes
@requires_scope CHARACTER_SKILLS

#### Parameters

##### options

`GetCharacterSkillsOptions`

#### Returns

`Promise`\<`CharacterSkills`\>

Skills object with categories: general, space, ground, social, science, light, dark, neutral

#### Example

```ts
const skills = await client.character.skills.list({ uid: '1:12345' });
skills.general?.[0].skill.forEach(s => console.log(s.attributes.type, s.value));
```

---

## Faction

# Class: FactionBudgetsResource

Faction budgets resource

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/uid/budgets/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionBudgetsResource**(`http`): `FactionBudgetsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionBudgetsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Budget`\>

Get a specific faction budget.

Returns the `Budget` object directly — not wrapped in a `Page`.

#### Parameters

##### options

###### budgetId

`string`

###### factionId

`string`

#### Returns

`Promise`\<`Budget`\>

The `Budget` entity.

#### Example

```ts
const budget = await client.faction.budgets.get({ factionId: '20:123', budgetId: 'budget-uid' });
console.log(budget); // access properties directly, not budget.data
```

***

### list()

> **list**(`options`): `Promise`\<`Page`\<`Budget`\>\>

List faction budgets (paginated)

#### Parameters

##### options

Faction ID and optional pagination parameters

###### factionId

`string`

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`Budget`\>\>

#### Example

```ts
const page = await client.faction.budgets.list({ factionId: '20:123' });
console.log(page.data); // Budget[]
// Iterate all pages:
for await (const budget of page) { console.log(budget); }
```

# Class: FactionCreditlogResource

Faction credit log resource

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/uid/creditlog/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionCreditlogResource**(`http`): `FactionCreditlogResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionCreditlogResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`Page`\<`CreditLogEntry`\>\>

Get faction credit log (paginated)

#### Parameters

##### options

Faction ID and optional pagination/filtering parameters

###### factionId

`string`

Faction UID

###### item_count?

`number`

Number of items to retrieve. Default: 50, Max: 1000

###### pageDelay?

`number`

Milliseconds to wait before fetching each subsequent page. Helps avoid rate limits during auto-pagination.

###### start_id?

`number`

Oldest transaction ID threshold (1 = oldest 1000, 0/default = newest 1000)

###### start_index?

`number`

Starting position (1-based). Default: 1

#### Returns

`Promise`\<`Page`\<`CreditLogEntry`\>\>

#### Example

```ts
const creditlog = await client.faction.creditlog.list({ factionId: '20:123' });
const moreLogs = await client.faction.creditlog.list({ factionId: '20:123', start_index: 51, item_count: 100 });
const oldestLogs = await client.faction.creditlog.list({ factionId: '20:123', start_id: 1 });
// Fetch up to 1000 credit log entries at once
const manyLogs = await client.faction.creditlog.list({ factionId: '20:123', item_count: 1000 });
```

# Class: FactionCreditsResource

Faction credits resource

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/uid/credits/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionCreditsResource**(`http`): `FactionCreditsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionCreditsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`FactionCredits`\>

Get faction credit balance.

Returns the `FactionCredits` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetFactionCreditsOptions`

#### Returns

`Promise`\<`FactionCredits`\>

The `FactionCredits` data.

#### Example

```ts
const credits = await client.faction.credits.get({ factionId: '20:123' });
console.log(credits); // access properties directly, not credits.data
```

***

### transfer()

> **transfer**(`options`): `Promise`\<`unknown`\>

Transfer faction credits

#### Parameters

##### options

`TransferFactionCreditsOptions`

#### Returns

`Promise`\<`unknown`\>

# Class: FactionMembersResource

Faction members resource

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/fuid/members/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionMembersResource**(`http`): `FactionMembersResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionMembersResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`Page`\<`FactionMember`\>\>

List faction members (paginated)

#### Parameters

##### options

Faction ID and optional pagination parameters

###### factionId

`string`

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`FactionMember`\>\>

#### Example

```ts
const page = await client.faction.members.list({ factionId: '20:123' });
console.log(page.data); // FactionMember[]
// Iterate all pages:
for await (const member of page) { console.log(member); }
```

***

### updateMemberInfo()

> **updateMemberInfo**(`options`): `Promise`\<`Record`\<`string`, `unknown`\>\>

Update faction member info field

#### Parameters

##### options

###### factionId

`string`

Faction UID

###### new_value

`string`

New value for the info field

###### property

`"info1"` \| `"info2"` \| `"info3"`

Which info field to update (info1, info2, or info3)

###### uid

`string`

Character UID to update

#### Returns

`Promise`\<`Record`\<`string`, `unknown`\>\>

# Class: FactionResource

Faction resource for managing factions

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/uid/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionResource**(`http`): `FactionResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### budgets

> `readonly` **budgets**: `FactionBudgetsResource`

***

### creditlog

> `readonly` **creditlog**: `FactionCreditlogResource`

***

### credits

> `readonly` **credits**: `FactionCreditsResource`

***

### members

> `readonly` **members**: `FactionMembersResource`

***

### stockholders

> `readonly` **stockholders**: `FactionStockholdersResource`

## Methods

### get()

> **get**(`options?`): `Promise`\<`FactionDetail`\>

Get faction by UID.

Returns the `FactionDetail` object directly — not wrapped in a `Page`.

@requires_auth Yes
@requires_scope FACTION_READ

#### Parameters

##### options?

`GetFactionOptions`

Optional faction UID. If omitted or uid not provided, returns the authenticated user's primary faction.

#### Returns

`Promise`\<`FactionDetail`\>

The `FactionDetail` entity.

#### Example

```ts
// Get a specific faction
const faction = await client.faction.get({ uid: '20:123' });
console.log(faction.name); // access properties directly, not faction.data

// Get the authenticated user's faction
const myFaction = await client.faction.get();
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`FactionListItem`\>\>

List all factions (paginated)
@requires_auth No

#### Parameters

##### options?

`ListFactionsOptions`

Optional pagination parameters

#### Returns

`Promise`\<`Page`\<`FactionListItem`\>\>

#### Example

```ts
const page = await client.faction.list();
console.log(page.total);
console.log(page.data[0]?.value);

// Iterate all pages:
for await (const faction of page) { console.log(faction); }
```

# Class: FactionStockholdersResource

Faction stockholders resource

## See

https://www.swcombine.com/ws/v2.0/documentation/faction/uid/stockholders/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new FactionStockholdersResource**(`http`): `FactionStockholdersResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`FactionStockholdersResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`Page`\<`Stockholder`\>\>

List faction stockholders (paginated)

#### Parameters

##### options

Faction ID and optional pagination parameters

###### factionId

`string`

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`Stockholder`\>\>

#### Example

```ts
const page = await client.faction.stockholders.list({ factionId: '20:123' });
console.log(page.data); // Stockholder[]
// Iterate all pages:
for await (const stockholder of page) { console.log(stockholder); }
```

---

## Galaxy

# Class: GalaxyCitiesResource

Galaxy cities resource

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/cities/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxyCitiesResource**(`http`): `GalaxyCitiesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxyCitiesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`City`\>

Get city by UID.

Returns the `City` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetCityOptions`

#### Returns

`Promise`\<`City`\>

The `City` entity.

#### Example

```ts
const city = await client.galaxy.cities.get({ uid: '13:123' });
console.log(city.name); // access properties directly, not city.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`GalaxyCityListItem`\>\>

List all cities (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`GalaxyCityListItem`\>\>

#### Example

```ts
const cities = await client.galaxy.cities.list();
const moreCities = await client.galaxy.cities.list({ start_index: 51, item_count: 50 });
```

# Class: GalaxyPlanetsResource

Galaxy planets resource

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/planets/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxyPlanetsResource**(`http`): `GalaxyPlanetsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxyPlanetsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Planet`\>

Get planet by UID.

Returns the `Planet` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetPlanetOptions`

#### Returns

`Promise`\<`Planet`\>

The `Planet` entity.

#### Example

```ts
const planet = await client.galaxy.planets.get({ uid: '9:123' });
console.log(planet.name); // access properties directly, not planet.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`GalaxyPlanetListItem`\>\>

List all planets (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`GalaxyPlanetListItem`\>\>

#### Example

```ts
const planets = await client.galaxy.planets.list();
const morePlanets = await client.galaxy.planets.list({ start_index: 51, item_count: 50 });
```

# Class: GalaxyResource

Galaxy resource for accessing galactic information

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/systems/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxyResource**(`http`): `GalaxyResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxyResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### cities

> `readonly` **cities**: `GalaxyCitiesResource`

***

### planets

> `readonly` **planets**: `GalaxyPlanetsResource`

***

### sectors

> `readonly` **sectors**: `GalaxySectorsResource`

***

### stations

> `readonly` **stations**: `GalaxyStationsResource`

***

### systems

> `readonly` **systems**: `GalaxySystemsResource`

## Methods

### getSectorsFromSystems()

> **getSectorsFromSystems**(): `Promise`\<`object`[]\>

Client-side helper method to extract unique sectors from systems list
Note: The API provides a direct sectors list endpoint via galaxy.sectors.list()
This method is an alternative that derives sector information from the systems endpoint

#### Returns

`Promise`\<`object`[]\>

Array of unique sector information extracted from systems

#### Example

```ts
const sectorsData = await client.galaxy.getSectorsFromSystems();
// Returns: [{ uid: '25:160', name: 'Seswenna', href: '...' }, ...]
```

# Class: GalaxySectorsResource

Galaxy sectors resource

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/sectors/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxySectorsResource**(`http`): `GalaxySectorsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxySectorsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Sector`\>

Get sector by name or UID.

Returns the `Sector` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetSectorOptions`

Sector identifier (use lowercase sector name, e.g., 'seswenna')

#### Returns

`Promise`\<`Sector`\>

The `Sector` entity.

#### Example

```ts
const sector = await client.galaxy.sectors.get({ uid: 'seswenna' });
console.log(sector.name); // access properties directly, not sector.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`GalaxySectorListItem`\>\>

List all sectors (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`GalaxySectorListItem`\>\>

#### Example

```ts
const sectors = await client.galaxy.sectors.list();
const moreSectors = await client.galaxy.sectors.list({ start_index: 51, item_count: 50 });
```

# Class: GalaxyStationsResource

Galaxy stations resource

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/stations/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxyStationsResource**(`http`): `GalaxyStationsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxyStationsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Station`\>

Get station by UID.

Returns the `Station` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetStationOptions`

#### Returns

`Promise`\<`Station`\>

The `Station` entity.

#### Example

```ts
const station = await client.galaxy.stations.get({ uid: '12:123' });
console.log(station.name); // access properties directly, not station.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`GalaxyStationListItem`\>\>

List all stations in named systems with no ECM (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`GalaxyStationListItem`\>\>

#### Example

```ts
const stations = await client.galaxy.stations.list();
const moreStations = await client.galaxy.stations.list({ start_index: 51, item_count: 50 });
```

# Class: GalaxySystemsResource

Galaxy systems resource

## See

https://www.swcombine.com/ws/v2.0/documentation/galaxy/systems/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GalaxySystemsResource**(`http`): `GalaxySystemsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GalaxySystemsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`System`\>

Get system by UID.

Returns the `System` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetSystemOptions`

#### Returns

`Promise`\<`System`\>

The `System` entity.

#### Example

```ts
const system = await client.galaxy.systems.get({ uid: '7:123' });
console.log(system.name); // access properties directly, not system.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`GalaxySystemListItem`\>\>

List all systems (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`GalaxySystemListItem`\>\>

#### Example

```ts
const systems = await client.galaxy.systems.list();
const moreSystems = await client.galaxy.systems.list({ start_index: 51, item_count: 50 });
```

---

## Inventory

# Class: InventoryEntitiesResource

Inventory entities resource

## See

https://www.swcombine.com/ws/v2.0/documentation/inventory/uid/entity_type/assign_type/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new InventoryEntitiesResource**(`http`): `InventoryEntitiesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`InventoryEntitiesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### addTag()

> **addTag**(`options`): `Promise`\<`void`\>

Add tag to entity

#### Parameters

##### options

###### entityType

`string`

###### tag

`string`

###### uid

`string`

#### Returns

`Promise`\<`void`\>

***

### get()

> **get**(`options`): `Promise`\<`Entity`\>

Get a specific inventory entity by type and UID.

Returns the `Entity` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetEntityOptions`

#### Returns

`Promise`\<`Entity`\>

The `Entity`.

#### Example

```ts
const ship = await client.inventory.entities.get({ entityType: 'ships', uid: '8:123' });
console.log(ship.name); // access properties directly, not ship.data
```

***

### list()

> **list**\<`T`\>(`options`): `Promise`\<`Page`\<`InventoryEntityTypeMap`\[`T`\]\>\>

List entities in inventory (paginated with optional filtering)

Supports filtering by various entity properties. Filter arrays must have matching lengths.

#### Type Parameters

##### T

`T` *extends* `InventoryEntityType`

#### Parameters

##### options

`ListInventoryEntitiesOptions`\<`T`\>

Inventory UID, entity type, assign type, and optional pagination/filtering parameters

#### Returns

`Promise`\<`Page`\<`InventoryEntityTypeMap`\[`T`\]\>\>

#### Example

```ts
const entities = await client.inventory.entities.list({ uid: '1:12345', entityType: 'vehicle', assignType: 'pilot' });
// Fetch up to 200 entities at once
const moreEntities = await client.inventory.entities.list({ uid: '1:12345', entityType: 'vehicle', assignType: 'pilot', start_index: 1, item_count: 200 });
// Filter by multiple criteria
const multiFiltered = await client.inventory.entities.list({
  uid: '1:12345',
  entityType: 'ships',
  assignType: 'owner',
  filter_type: ['class', 'powered'],
  filter_value: ['Fighter', '1'],
  filter_inclusion: ['includes', 'includes']
});
```

***

### removeAllTags()

> **removeAllTags**(`options`): `Promise`\<`void`\>

Remove all tags from entity

#### Parameters

##### options

###### entityType

`string`

###### uid

`string`

#### Returns

`Promise`\<`void`\>

***

### removeTag()

> **removeTag**(`options`): `Promise`\<`void`\>

Remove tag from entity

#### Parameters

##### options

###### entityType

`string`

###### tag

`string`

###### uid

`string`

#### Returns

`Promise`\<`void`\>

***

### updateProperty()

> **updateProperty**(`options`): `Promise`\<`Entity`\>

Update entity property

#### Parameters

##### options

###### entityType

`string`

Entity type (ships, vehicles, stations, etc.)

###### new_value

`string`

New value for the property

###### property

`"name"` \| `"owner"` \| `"infotext"` \| `"commander"` \| `"pilot"` \| `"open-to"` \| `"action"` \| `"crewlist-add"` \| `"crewlist-remove"` \| `"crewlist-clear"`

Property to update

###### reason?

`string`

Optional reason for the change

###### uid

`string`

Entity UID

#### Returns

`Promise`\<`Entity`\>

# Class: InventoryResource

Inventory resource for managing inventories

## See

https://www.swcombine.com/ws/v2.0/documentation/inventory/uid/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new InventoryResource**(`http`): `InventoryResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`InventoryResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### entities

> `readonly` **entities**: `InventoryEntitiesResource`

## Methods

### get()

> **get**(`options`): `Promise`\<`Record`\<`string`, `unknown`\>\>

Get inventory summary by UID.

Returns the inventory summary object directly — not wrapped in a `Page`.

#### Parameters

##### options

###### uid

`string`

#### Returns

`Promise`\<`Record`\<`string`, `unknown`\>\>

The inventory summary.

#### Example

```ts
const overview = await client.inventory.get({ uid: '1:12345' });
console.log(overview); // access properties directly, not overview.data
```

---

## Market

# Class: MarketResource

Market resource for accessing market data

## See

https://www.swcombine.com/ws/v2.0/documentation/market/vendors/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new MarketResource**(`http`): `MarketResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`MarketResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### vendors

> `readonly` **vendors**: `MarketVendorsResource`

# Class: MarketVendorsResource

Market vendors resource

## See

https://www.swcombine.com/ws/v2.0/documentation/market/vendors/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new MarketVendorsResource**(`http`): `MarketVendorsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`MarketVendorsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Vendor`\>

Get a specific vendor by UID.

Returns the `Vendor` object directly — not wrapped in a `Page`.

#### Parameters

##### options

`GetVendorOptions`

#### Returns

`Promise`\<`Vendor`\>

The `Vendor` entity.

#### Example

```ts
const vendor = await client.market.vendors.get({ uid: 'vendor-uid' });
console.log(vendor.name); // access properties directly, not vendor.data
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`Vendor`\>\>

List all public vendors (paginated)

#### Parameters

##### options?

Optional pagination parameters

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`Vendor`\>\>

#### Example

```ts
const vendors = await client.market.vendors.list();
const moreVendors = await client.market.vendors.list({ start_index: 51, item_count: 50 });
```

---

## News

# Class: GNSResource

Galactic News Service (GNS) resource

## See

https://www.swcombine.com/ws/v2.0/documentation/news/gns/category/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new GNSResource**(`http`): `GNSResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`GNSResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`NewsItem`\>

Get a specific GNS news item by numeric ID.

Returns the `NewsItem` object directly — not wrapped in a `Page`.
Author and faction are normalized to object references with `value`.
If the API returns an array, the SDK returns the first normalized item.

#### Parameters

##### options

`GetNewsItemOptions`

#### Returns

`Promise`\<`NewsItem`\>

The `NewsItem` entity.

#### Example

```ts
const post = await client.news.gns.get({ id: 49108 });
console.log(post.title);          // access properties directly, not post.data
console.log(post.author.value);
console.log(post.faction.value);
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`NewsListItem`\>\>

List GNS news items (paginated with optional filtering)
Returns a Page of headline metadata entries (`attributes.id`, `attributes.href`, `attributes.title`, `value`).
Use `page.data` for the current page items, `page.hasMore` / `page.getNextPage()` for pagination,
or `for await (const item of page)` to iterate through all pages automatically.
@requires_auth No

#### Parameters

##### options?

`ListGNSOptions`

Optional category, pagination, and filtering parameters

#### Returns

`Promise`\<`Page`\<`NewsListItem`\>\>

#### Example

```ts
const page = await client.news.gns.list();
const economyNews = await client.news.gns.list({ category: 'economy' });
const moreNews = await client.news.gns.list({ start_index: 51, item_count: 50 });
const searchNews = await client.news.gns.list({ search: 'battle', author: 'John Doe' });
const factionNews = await client.news.gns.list({ faction: 'Empire', faction_type: 'government' });
console.log(page.data[0].attributes.title);
console.log(page.start, page.total, page.count);
```

# Class: NewsResource

News resource for accessing news feeds

## See

https://www.swcombine.com/ws/v2.0/documentation/news/gns/category/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new NewsResource**(`http`): `NewsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`NewsResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### gns

> `readonly` **gns**: `GNSResource`

***

### simNews

> `readonly` **simNews**: `SimNewsResource`

# Class: SimNewsResource

Sim News resource

## See

https://www.swcombine.com/ws/v2.0/documentation/news/simnews/category/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new SimNewsResource**(`http`): `SimNewsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`SimNewsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`NewsItem`\>

Get a specific Sim News item by numeric ID.

Returns the `NewsItem` object directly — not wrapped in a `Page`.
Author and faction are normalized to object references with `value`.
If the API returns an array, the SDK returns the first normalized item.

#### Parameters

##### options

`GetNewsItemOptions`

#### Returns

`Promise`\<`NewsItem`\>

The `NewsItem` entity.

#### Example

```ts
const post = await client.news.simNews.get({ id: 49108 });
console.log(post.title);          // access properties directly, not post.data
console.log(post.author.value);
console.log(post.faction.value);
```

***

### list()

> **list**(`options?`): `Promise`\<`Page`\<`NewsListItem`\>\>

List Sim News items (paginated with optional filtering)
Returns a Page of headline metadata entries (`attributes.id`, `attributes.href`, `attributes.title`, `value`).
Use `page.data` for the current page items, `page.hasMore` / `page.getNextPage()` for pagination,
or `for await (const item of page)` to iterate through all pages automatically.
@requires_auth No

#### Parameters

##### options?

`ListNewsOptionsBase`

Optional category, pagination, and filtering parameters

#### Returns

`Promise`\<`Page`\<`NewsListItem`\>\>

#### Example

```ts
const page = await client.news.simNews.list();
const playerNews = await client.news.simNews.list({ category: 'player' });
const moreNews = await client.news.simNews.list({ start_index: 51, item_count: 50 });
const searchNews = await client.news.simNews.list({ search: 'update', author: 'Admin' });
console.log(page.data[0].attributes.title);
console.log(page.start, page.total, page.count);
```

---

## Types

# Class: TypesClassesResource

Entity classes resource

## See

https://www.swcombine.com/ws/v2.0/documentation/types/classes/entity_type/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new TypesClassesResource**(`http`): `TypesClassesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`TypesClassesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### list()

> **list**(`options`): `Promise`\<`Page`\<`EntityClass`\>\>

Get all classes for a specific entity type (paginated)

NOTE: Official API documentation states "Query String: N/A" and "Parameters: N/A",
but empirical testing suggests pagination parameters may be required to avoid 404 errors.
The SDK includes pagination parameters by default for safety.

#### Parameters

##### options

Entity type (plural, e.g., 'vehicles', 'ships') and optional pagination parameters

###### entityType

`TypesEntityType`

###### item_count?

`number`

###### pageDelay?

`number`

###### start_index?

`number`

#### Returns

`Promise`\<`Page`\<`EntityClass`\>\>

#### Example

```ts
const classes = await client.types.classes.list({ entityType: 'vehicles' });
const moreClasses = await client.types.classes.list({ entityType: 'vehicles', start_index: 51, item_count: 50 });
```

# Class: TypesEntitiesResource

Entities by type resource

## See

https://www.swcombine.com/ws/v2.0/documentation/types/entity_type/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new TypesEntitiesResource**(`http`): `TypesEntitiesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`TypesEntitiesResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**\<`T`\>(`options`): `Promise`\<`TypesEntityGetResponseMap`\[`T`\]\>

Get specific entity type information by category and UID.

Returns the entity type object directly — not wrapped in a `Page`.

#### Type Parameters

##### T

`T` *extends* `TypesEntityType`

#### Parameters

##### options

`GetTypesEntityOptions`\<`T`\>

#### Returns

`Promise`\<`TypesEntityGetResponseMap`\[`T`\]\>

The entity type detail (e.g. `TypesShipEntity`, `TypesVehicleEntity`, etc.).

#### Example

```ts
const shipType = await client.types.entities.get({ entityType: 'ships', uid: '8:7' });
console.log(shipType.name); // access properties directly, not shipType.data
```

***

### list()

> **list**\<`T`\>(`options`): `Promise`\<`Page`\<`TypesEntityListItem`\>\>

Get all entities of a type (paginated)

#### Type Parameters

##### T

`T` *extends* `TypesEntityType`

#### Parameters

##### options

`ListTypesEntitiesOptions`\<`T`\>

Entity type (plural, e.g., 'vehicles', 'ships'), optional class filter, and optional pagination parameters

#### Returns

`Promise`\<`Page`\<`TypesEntityListItem`\>\>

#### Example

```ts
const ships = await client.types.entities.list({ entityType: 'ships' });
const moreShips = await client.types.entities.list({ entityType: 'ships', start_index: 51, item_count: 50 });
const fighters = await client.types.entities.list({ entityType: 'ships', class: 'fighter', start_index: 1, item_count: 50 });
```

# Class: TypesResource

Types resource for accessing type information

## See

https://www.swcombine.com/ws/v2.0/documentation/types/entitytypes/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new TypesResource**(`http`): `TypesResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`TypesResource`

#### Overrides

`BaseResource`.`constructor`

## Properties

### classes

> `readonly` **classes**: `TypesClassesResource`

***

### entities

> `readonly` **entities**: `TypesEntitiesResource`

## Methods

### listEntityTypes()

> **listEntityTypes**(): `Promise`\<`EntityType`[]\>

Get all entity types

#### Returns

`Promise`\<`EntityType`[]\>

---

## Events

# Class: EventsResource

Events resource for querying events

## See

https://www.swcombine.com/ws/v2.0/documentation/events/event_mode/event_type/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new EventsResource**(`http`): `EventsResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`EventsResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Event`\>

Get a specific event by UID.

Returns the `Event` object directly — not wrapped in a `Page`.

#### Parameters

##### options

###### uid

`string`

#### Returns

`Promise`\<`Event`\>

The `Event` entity.

#### Example

```ts
const event = await client.events.get({ uid: '5:99001' });
console.log(event.type); // access properties directly, not event.data
```

***

### list()

> **list**(`options`): `Promise`\<`Page`\<`Event`\>\>

List events by type and mode (paginated)

**Note:** This endpoint uses 0-based indexing (unlike most other endpoints which use 1-based).

#### Parameters

##### options

Event mode, event type, and optional pagination/filtering parameters

###### eventMode

`string`

Event mode: 'personal', 'faction', 'inventory', or 'combat'

###### eventType?

`string`

Event type filter (optional, only for personal/faction modes)

###### faction_id?

`string`

Faction ID for faction mode

###### item_count?

`number`

Number of items to retrieve. Default: 50, Max: 1000

###### pageDelay?

`number`

Milliseconds to wait before fetching each subsequent page. Helps avoid rate limits during auto-pagination.

###### start_index?

`number`

Starting position (0-based). Default: 0

###### start_time?

`number`

Unix timestamp to filter events after this time

#### Returns

`Promise`\<`Page`\<`Event`\>\>

#### Example

```ts
const events = await client.events.list({ eventMode: 'personal' });
const moreEvents = await client.events.list({ eventMode: 'personal', start_index: 50, item_count: 100 });
const recentEvents = await client.events.list({ eventMode: 'personal', start_time: 1640000000 });
const factionEvents = await client.events.list({ eventMode: 'faction', faction_id: '20:123' });
// Fetch up to 1000 events at once
const manyEvents = await client.events.list({ eventMode: 'personal', item_count: 1000 });
```

---

## Location

# Class: LocationResource

Location resource for querying entity locations

## See

https://www.swcombine.com/ws/v2.0/documentation/location/entity_type/uid/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new LocationResource**(`http`): `LocationResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`LocationResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### get()

> **get**(`options`): `Promise`\<`Location`\>

Get an entity's current location.

Returns the `Location` object directly — not wrapped in a `Page`.

#### Parameters

##### options

###### entityType

`string`

###### uid

`string`

#### Returns

`Promise`\<`Location`\>

The `Location` data.

#### Example

```ts
const location = await client.location.get({ entityType: 'character', uid: '1:12345' });
console.log(location); // access properties directly, not location.data
```

---

## Datacard

# Class: DatacardResource

Datacard resource for managing datacards

## See

https://www.swcombine.com/ws/v2.0/documentation/datacard/uid/ SW Combine API Documentation

## Extends

- `BaseResource`

## Constructors

### Constructor

> **new DatacardResource**(`http`): `DatacardResource`

#### Parameters

##### http

`HttpClient`

#### Returns

`DatacardResource`

#### Inherited from

`BaseResource`.`constructor`

## Methods

### create()

> **create**(`options`): `Promise`\<`Datacard`\>

Create/assign datacard to production entity

#### Parameters

##### options

###### production_entity_uid

`string`

The entity UID to assign this datacard to

###### uid

`string`

Datacard UID

###### unlimited?

`boolean`

Set to true for unlimited uses (optional, use this OR uses)

###### uses?

`number`

Number of uses (optional, use this OR unlimited)

#### Returns

`Promise`\<`Datacard`\>

***

### delete()

> **delete**(`options`): `Promise`\<`void`\>

Delete datacard assignment

#### Parameters

##### options

###### production_entity_uid

`string`

Production entity UID to revoke from (required)

###### uid

`string`

Datacard UID

#### Returns

`Promise`\<`void`\>

***

### get()

> **get**(`options`): `Promise`\<`Datacard`\>

Get a specific datacard by UID.

Returns the `Datacard` object directly — not wrapped in a `Page`.
Access properties like `.name` and `.uid` on the result itself.

#### Parameters

##### options

###### uid

`string`

#### Returns

`Promise`\<`Datacard`\>

The `Datacard` entity.

#### Example

```typescript
const card = await client.datacard.get({ uid: 'datacard-uid' });
console.log(card.name); // access properties directly, not card.data
```

***

### list()

> **list**(`options`): `Promise`\<`Page`\<`Datacard`\>\>

List datacards owned by faction.

Returns a `Page<Datacard>` — access the array of datacards via `.data`.

#### Parameters

##### options

###### factionId

`string`

###### pageDelay?

`number`

#### Returns

`Promise`\<`Page`\<`Datacard`\>\>

A `Page<Datacard>` with `.data`, `.total`, `.hasMore`, and `.getNextPage()`.

#### Example

```typescript
const page = await client.datacard.list({ factionId: '20:123' });
console.log(page.data);   // Datacard[] — items on this page
console.log(page.total);  // total datacards across all pages

for await (const card of page) {
  console.log(card.name); // auto-paginates
}
```

---

## Utilities

# Class: Timestamp

Utility class for working with Star Wars Combine timestamps.
Represents Combine Galactic Time and can convert unix timestamps
and Date objects to/from CGT.

## Constructors

### Constructor

> **new Timestamp**(`source`): `Timestamp`

Create a new Timestamp object for a specific moment in Combine Galactic Time

#### Parameters

##### source

`TimestampMoment`

Moment in CGT (year, day, and optionally hour, minute, second)

#### Returns

`Timestamp`

## Methods

### add()

> **add**(`duration`): `Timestamp`

Calculate a new timestamp by adding time to this timestamp

#### Parameters

##### duration

`Partial`\<`Duration`\>

Duration to add (partial duration allowed)

#### Returns

`Timestamp`

New Timestamp with added duration

***

### asMoment()

> **asMoment**(): `TimestampMoment`

Get the timestamp as a TimestampMoment object

#### Returns

`TimestampMoment`

TimestampMoment representation

***

### getDay()

> **getDay**(): `number`

Get the day component

#### Returns

`number`

Day of the year (1-365)

***

### getDurationTo()

> **getDurationTo**(`otherTimestamp`): `Duration`

Calculate the duration from this timestamp to another timestamp

#### Parameters

##### otherTimestamp

`Timestamp`

Target timestamp

#### Returns

`Duration`

Duration between timestamps (positive if other is in future)

***

### getHour()

> **getHour**(): `number`

Get the hour component

#### Returns

`number`

Hour of the day (0-23)

***

### getMinute()

> **getMinute**(): `number`

Get the minute component

#### Returns

`number`

Minute of the hour (0-59)

***

### getSecond()

> **getSecond**(): `number`

Get the second component

#### Returns

`number`

Second of the minute (0-59)

***

### getYear()

> **getYear**(): `number`

Get the year component

#### Returns

`number`

Year in CGT

***

### subtract()

> **subtract**(`duration`): `Timestamp`

Calculate a new timestamp by subtracting time from this timestamp

#### Parameters

##### duration

`Partial`\<`Duration`\>

Duration to subtract (partial duration allowed)

#### Returns

`Timestamp`

New Timestamp with subtracted duration (won't go before SWC start)

***

### toDate()

> **toDate**(): `Date`

Convert the CGT timestamp into a Date object

#### Returns

`Date`

JavaScript Date object

***

### toString()

> **toString**(`format?`): `string`

Convert the CGT timestamp to a string

You can either pass in a preset name, or a custom format string.

**Preset formats:**
- `'full'`: Year 25 Day 60, 6:03:12
- `'minute'`: Year 25 Day 60, 6:03
- `'day'`: Year 25 Day 60
- `'shortFull'`: Y25 D60, 6:03:12
- `'shortMinute'`: Y25 D60, 6:03
- `'shortDay'`: Y26 D60

**Custom format tags:**
- `{y}`: year
- `{d}`: day
- `{h}`: hour
- `{m}`: minute
- `{s}`: second
- Double the tag for leading zeroes (e.g., `{hh}` = 08)
- `{hms}`: shorthand for `{hh}:{mm}:{ss}`

#### Parameters

##### format?

`string` = `'full'`

Preset format name or custom format string

#### Returns

`string`

Formatted timestamp string

***

### toUnixTimestamp()

> **toUnixTimestamp**(`unit`): `number`

Convert the CGT timestamp into a unix timestamp

#### Parameters

##### unit

`TimestampUnit`

Output unit (seconds or milliseconds)

#### Returns

`number`

Unix timestamp

***

### fromDate()

> `static` **fromDate**(`date`): `Timestamp`

Convert a Date object into Combine Galactic Time

#### Parameters

##### date

`Date`

JavaScript Date object

#### Returns

`Timestamp`

Timestamp instance representing the CGT moment

***

### fromUnixTimestamp()

> `static` **fromUnixTimestamp**(`unixTimestamp`): `Timestamp`

Convert a unix timestamp to Combine Galactic Time

#### Parameters

##### unixTimestamp

`number`

Unix timestamp (seconds or milliseconds, auto-detected)

#### Returns

`Timestamp`

Timestamp instance representing the CGT moment

***

### now()

> `static` **now**(): `Timestamp`

Get the current Combine Galactic Time

#### Returns

`Timestamp`

Timestamp instance representing the current CGT moment

# Class: SWCError

Custom error class for SW Combine API errors

## Extends

- `Error`

## Constructors

### Constructor

> **new SWCError**(`message`, `options`): `SWCError`

#### Parameters

##### message

`string`

##### options

`SWCErrorOptions`

#### Returns

`SWCError`

#### Overrides

`Error.constructor`

## Properties

### cause?

> `readonly` `optional` **cause**: `Error`

Original error that caused this error

***

### message

> **message**: `string`

#### Inherited from

`Error.message`

***

### name

> **name**: `string`

#### Inherited from

`Error.name`

***

### requestId?

> `readonly` `optional` **requestId**: `string`

Request ID from API response

***

### response?

> `readonly` `optional` **response**: `Record`\<`string`, `unknown`\>

Original API error response

***

### retryable

> `readonly` **retryable**: `boolean`

Whether this error can be retried

***

### retryAfter?

> `readonly` `optional` **retryAfter**: `number`

Seconds to wait before retrying (for rate limit errors)

***

### stack?

> `optional` **stack**: `string`

#### Inherited from

`Error.stack`

***

### statusCode?

> `readonly` `optional` **statusCode**: `number`

HTTP status code if applicable

***

### type

> `readonly` **type**: `SWCErrorType`

Error type discriminator

***

### stackTraceLimit

> `static` **stackTraceLimit**: `number`

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

#### Inherited from

`Error.stackTraceLimit`

## Methods

### toUserFriendlyMessage()

> **toUserFriendlyMessage**(): `string`

Get a user-friendly error message

#### Returns

`string`

***

### captureStackTrace()

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

#### Parameters

##### targetObject

`object`

##### constructorOpt?

`Function`

#### Returns

`void`

#### Inherited from

`Error.captureStackTrace`

***

### fromHttpResponse()

> `static` **fromHttpResponse**(`statusCode`, `response`, `requestId?`): `SWCError`

Create an error from an HTTP status code and response

#### Parameters

##### statusCode

`number`

##### response

`Record`\<`string`, `unknown`\> | `undefined`

##### requestId?

`string`

#### Returns

`SWCError`

***

### fromNetworkError()

> `static` **fromNetworkError**(`error`): `SWCError`

Create an error from a network failure

#### Parameters

##### error

`Error`

#### Returns

`SWCError`

***

### isSWCError()

> `static` **isSWCError**(`error`): `error is SWCError`

Check if an error is a SWCError

#### Parameters

##### error

`unknown`

#### Returns

`error is SWCError`

***

### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

#### Parameters

##### err

`Error`

##### stackTraces

`CallSite`[]

#### Returns

`any`

#### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

#### Inherited from

`Error.prepareStackTrace`

---

## Configuration Types

# Interface: ClientConfig

## Properties

### accessType?

> `optional` **accessType**: `AccessType`

Access type: online or offline (offline provides refresh token)

***

### baseURL?

> `optional` **baseURL**: `string`

Base URL for API (default: https://www.swcombine.com/ws/v2.0/)

***

### clientId?

> `optional` **clientId**: `string`

OAuth client ID

***

### clientSecret?

> `optional` **clientSecret**: `string`

OAuth client secret

***

### debug?

> `optional` **debug**: `boolean`

Enable debug logging

***

### maxRetries?

> `optional` **maxRetries**: `number`

Maximum number of retry attempts (default: 3)

***

### redirectUri?

> `optional` **redirectUri**: `string`

OAuth redirect URI

***

### retryDelay?

> `optional` **retryDelay**: `number`

Delay between retries in milliseconds (default: 1000)

***

### timeout?

> `optional` **timeout**: `number`

Request timeout in milliseconds (default: 30000)

***

### token?

> `optional` **token**: `string` \| `OAuthToken`

Existing token to initialize with

# Interface: OAuthToken

## Properties

### accessToken

> **accessToken**: `string`

Access token for API requests

***

### expiresAt

> **expiresAt**: `number`

Token expiration timestamp (milliseconds since epoch)

***

### refreshToken?

> `optional` **refreshToken**: `string`

Refresh token for getting new access token

# Interface: OAuthAuthorizationOptions

## Properties

### scopes

> **scopes**: `AllScopes`[]

OAuth scopes to request

***

### state

> **state**: `string`

State parameter for CSRF protection

# Interface: OAuthCallbackQuery

## Properties

### code?

> `optional` **code**: `string`

Authorization code from callback

***

### error?

> `optional` **error**: `string`

Error from callback

***

### error\_description?

> `optional` **error\_description**: `string`

Error description

***

### state?

> `optional` **state**: `string`

State parameter echoed back

---

## OAuth Scopes

# Variable: CharacterScopes

> `const` **CharacterScopes**: `object`

Character-related scopes

## Type Declaration

### ALL

> `readonly` **ALL**: `"character_all"` = `'character_all'`

Access all character information

### AUTH

> `readonly` **AUTH**: `"character_auth"` = `'character_auth'`

Solely provides the character name and ID for use by clients who want to verify a character's identity

### CREDITS

> `readonly` **CREDITS**: `"character_credits"` = `'character_credits'`

Read character credit information

### CREDITS\_WRITE

> `readonly` **CREDITS\_WRITE**: `"character_credits_write"` = `'character_credits_write'`

Transfer character credits

### EVENTS

> `readonly` **EVENTS**: `"character_events"` = `'character_events'`

Read the characters' events

### FORCE

> `readonly` **FORCE**: `"character_force"` = `'character_force'`

Read character force-related information.<br/>If you are Force-aware, this will return Master or Student where applicable, as well as FP, FXP, regen rate, and Force Meter.

### LOCATION

> `readonly` **LOCATION**: `"character_location"` = `'character_location'`

Read your location in-game

### PRIVILEGES

> `readonly` **PRIVILEGES**: `"character_privileges"` = `'character_privileges'`

Read character privileges

### READ

> `readonly` **READ**: `"character_read"` = `'character_read'`

Read basic character information.<br/>If your profile is private, this will return unique ID, handle, and image.<br/>If your profile is not private, this will also return your race, gender, last login, description, and biography.

### SKILLS

> `readonly` **SKILLS**: `"character_skills"` = `'character_skills'`

Read character skills

### STATS

> `readonly` **STATS**: `"character_stats"` = `'character_stats'`

Read information about the character's HP and XP

# Variable: MessageScopes

> `const` **MessageScopes**: `object`

Message-related scopes

## Type Declaration

### ALL

> `readonly` **ALL**: `"messages_all"` = `'messages_all'`

Read, send and delete messages to or from the character

### DELETE

> `readonly` **DELETE**: `"messages_delete"` = `'messages_delete'`

Delete messages to or from the character

### READ

> `readonly` **READ**: `"messages_read"` = `'messages_read'`

Read messages sent to or by the character

### SEND

> `readonly` **SEND**: `"messages_send"` = `'messages_send'`

Send messages to or from the character

# Variable: FactionScopes

> `const` **FactionScopes**: `object`

Faction management scopes

## Type Declaration

### ALL

> `readonly` **ALL**: `"faction_all"` = `'faction_all'`

Access and change anything in your faction

### BUDGETS\_READ

> `readonly` **BUDGETS\_READ**: `"faction_budgets_read"` = `'faction_budgets_read'`

See your faction's budgets

### BUDGETS\_WRITE

> `readonly` **BUDGETS\_WRITE**: `"faction_budgets_write"` = `'faction_budgets_write'`

Change your faction's budgets

### CREDITS\_READ

> `readonly` **CREDITS\_READ**: `"faction_credits_read"` = `'faction_credits_read'`

See how many credits your faction owns

### CREDITS\_WRITE

> `readonly` **CREDITS\_WRITE**: `"faction_credits_write"` = `'faction_credits_write'`

Transfer credits on behalf of your faction

### DATACARDS\_READ

> `readonly` **DATACARDS\_READ**: `"faction_datacards_read"` = `'faction_datacards_read'`

See your faction's datacard assignments

### DATACARDS\_WRITE

> `readonly` **DATACARDS\_WRITE**: `"faction_datacards_write"` = `'faction_datacards_write'`

Assign or revoke your faction's datacard assignments

### MEMBERS

> `readonly` **MEMBERS**: `"faction_members"` = `'faction_members'`

See your faction's members list

### READ

> `readonly` **READ**: `"faction_read"` = `'faction_read'`

Read basic information about your faction

### STOCKS

> `readonly` **STOCKS**: `"faction_stocks"` = `'faction_stocks'`

See the stocks your faction owns

# Variable: Scopes

> `const` **Scopes**: `object`

All available scopes organized by category

## Type Declaration

### Character

> `readonly` **Character**: `object` = `CharacterScopes`

#### Character.ALL

> `readonly` **ALL**: `"character_all"` = `'character_all'`

Access all character information

#### Character.AUTH

> `readonly` **AUTH**: `"character_auth"` = `'character_auth'`

Solely provides the character name and ID for use by clients who want to verify a character's identity

#### Character.CREDITS

> `readonly` **CREDITS**: `"character_credits"` = `'character_credits'`

Read character credit information

#### Character.CREDITS\_WRITE

> `readonly` **CREDITS\_WRITE**: `"character_credits_write"` = `'character_credits_write'`

Transfer character credits

#### Character.EVENTS

> `readonly` **EVENTS**: `"character_events"` = `'character_events'`

Read the characters' events

#### Character.FORCE

> `readonly` **FORCE**: `"character_force"` = `'character_force'`

Read character force-related information.<br/>If you are Force-aware, this will return Master or Student where applicable, as well as FP, FXP, regen rate, and Force Meter.

#### Character.LOCATION

> `readonly` **LOCATION**: `"character_location"` = `'character_location'`

Read your location in-game

#### Character.PRIVILEGES

> `readonly` **PRIVILEGES**: `"character_privileges"` = `'character_privileges'`

Read character privileges

#### Character.READ

> `readonly` **READ**: `"character_read"` = `'character_read'`

Read basic character information.<br/>If your profile is private, this will return unique ID, handle, and image.<br/>If your profile is not private, this will also return your race, gender, last login, description, and biography.

#### Character.SKILLS

> `readonly` **SKILLS**: `"character_skills"` = `'character_skills'`

Read character skills

#### Character.STATS

> `readonly` **STATS**: `"character_stats"` = `'character_stats'`

Read information about the character's HP and XP

### Faction

> `readonly` **Faction**: `object` = `FactionScopes`

#### Faction.ALL

> `readonly` **ALL**: `"faction_all"` = `'faction_all'`

Access and change anything in your faction

#### Faction.BUDGETS\_READ

> `readonly` **BUDGETS\_READ**: `"faction_budgets_read"` = `'faction_budgets_read'`

See your faction's budgets

#### Faction.BUDGETS\_WRITE

> `readonly` **BUDGETS\_WRITE**: `"faction_budgets_write"` = `'faction_budgets_write'`

Change your faction's budgets

#### Faction.CREDITS\_READ

> `readonly` **CREDITS\_READ**: `"faction_credits_read"` = `'faction_credits_read'`

See how many credits your faction owns

#### Faction.CREDITS\_WRITE

> `readonly` **CREDITS\_WRITE**: `"faction_credits_write"` = `'faction_credits_write'`

Transfer credits on behalf of your faction

#### Faction.DATACARDS\_READ

> `readonly` **DATACARDS\_READ**: `"faction_datacards_read"` = `'faction_datacards_read'`

See your faction's datacard assignments

#### Faction.DATACARDS\_WRITE

> `readonly` **DATACARDS\_WRITE**: `"faction_datacards_write"` = `'faction_datacards_write'`

Assign or revoke your faction's datacard assignments

#### Faction.MEMBERS

> `readonly` **MEMBERS**: `"faction_members"` = `'faction_members'`

See your faction's members list

#### Faction.READ

> `readonly` **READ**: `"faction_read"` = `'faction_read'`

Read basic information about your faction

#### Faction.STOCKS

> `readonly` **STOCKS**: `"faction_stocks"` = `'faction_stocks'`

See the stocks your faction owns

### FactionInventory

> `readonly` **FactionInventory**: `object` = `FactionInventoryScopes`

#### FactionInventory.CITIES

> `readonly` **CITIES**: `object`

Faction city scopes

#### FactionInventory.CITIES.ALL

> `readonly` **ALL**: `"faction_inv_cities_all"` = `'faction_inv_cities_all'`

Access and change anything in your faction's city inventory

#### FactionInventory.CITIES.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_cities_assign"` = `'faction_inv_cities_assign'`

Assign your faction's cities

#### FactionInventory.CITIES.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_cities_makeover"` = `'faction_inv_cities_makeover'`

Make over your faction's cities

#### FactionInventory.CITIES.READ

> `readonly` **READ**: `"faction_inv_cities_read"` = `'faction_inv_cities_read'`

Read basic information about your faction's cities

#### FactionInventory.CITIES.RENAME

> `readonly` **RENAME**: `"faction_inv_cities_rename"` = `'faction_inv_cities_rename'`

Rename your faction's cities

#### FactionInventory.CITIES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_cities_tags_read"` = `'faction_inv_cities_tags_read'`

Read the tags assigned to your faction's cities

#### FactionInventory.CITIES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_cities_tags_write"` = `'faction_inv_cities_tags_write'`

Modify the tags assigned to your faction's cities

#### FactionInventory.CREATURES

> `readonly` **CREATURES**: `object`

Faction creature scopes

#### FactionInventory.CREATURES.ALL

> `readonly` **ALL**: `"faction_inv_creatures_all"` = `'faction_inv_creatures_all'`

Access and change anything in your faction's creature inventory

#### FactionInventory.CREATURES.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_creatures_assign"` = `'faction_inv_creatures_assign'`

Assign your faction's creatures

#### FactionInventory.CREATURES.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_creatures_makeover"` = `'faction_inv_creatures_makeover'`

Make over your faction's creatures

#### FactionInventory.CREATURES.READ

> `readonly` **READ**: `"faction_inv_creatures_read"` = `'faction_inv_creatures_read'`

Read basic information about your faction's creatures

#### FactionInventory.CREATURES.RENAME

> `readonly` **RENAME**: `"faction_inv_creatures_rename"` = `'faction_inv_creatures_rename'`

Rename your faction's creatures

#### FactionInventory.CREATURES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_creatures_tags_read"` = `'faction_inv_creatures_tags_read'`

Read the tags assigned to your faction's creatures

#### FactionInventory.CREATURES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_creatures_tags_write"` = `'faction_inv_creatures_tags_write'`

Modify the tags assigned to your faction's creatures

#### FactionInventory.DROIDS

> `readonly` **DROIDS**: `object`

Faction droid scopes

#### FactionInventory.DROIDS.ALL

> `readonly` **ALL**: `"faction_inv_droids_all"` = `'faction_inv_droids_all'`

Access and change anything in your faction's droid inventory

#### FactionInventory.DROIDS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_droids_assign"` = `'faction_inv_droids_assign'`

Assign your faction's droids

#### FactionInventory.DROIDS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_droids_makeover"` = `'faction_inv_droids_makeover'`

Make over your faction's droids

#### FactionInventory.DROIDS.READ

> `readonly` **READ**: `"faction_inv_droids_read"` = `'faction_inv_droids_read'`

Read basic information about your faction's droids

#### FactionInventory.DROIDS.RENAME

> `readonly` **RENAME**: `"faction_inv_droids_rename"` = `'faction_inv_droids_rename'`

Rename your faction's droids

#### FactionInventory.DROIDS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_droids_tags_read"` = `'faction_inv_droids_tags_read'`

Read the tags assigned to your faction's droids

#### FactionInventory.DROIDS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_droids_tags_write"` = `'faction_inv_droids_tags_write'`

Modify the tags assigned to your faction's droids

#### FactionInventory.FACILITIES

> `readonly` **FACILITIES**: `object`

Faction facility scopes

#### FactionInventory.FACILITIES.ALL

> `readonly` **ALL**: `"faction_inv_facilities_all"` = `'faction_inv_facilities_all'`

Access and change anything in your faction's facility inventory

#### FactionInventory.FACILITIES.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_facilities_assign"` = `'faction_inv_facilities_assign'`

Assign your faction's facilities

#### FactionInventory.FACILITIES.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_facilities_makeover"` = `'faction_inv_facilities_makeover'`

Make over your faction's facilities

#### FactionInventory.FACILITIES.READ

> `readonly` **READ**: `"faction_inv_facilities_read"` = `'faction_inv_facilities_read'`

Read basic information about your faction's facilities

#### FactionInventory.FACILITIES.RENAME

> `readonly` **RENAME**: `"faction_inv_facilities_rename"` = `'faction_inv_facilities_rename'`

Rename your faction's facilities

#### FactionInventory.FACILITIES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_facilities_tags_read"` = `'faction_inv_facilities_tags_read'`

Read the tags assigned to your faction's facilities

#### FactionInventory.FACILITIES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_facilities_tags_write"` = `'faction_inv_facilities_tags_write'`

Modify the tags assigned to your faction's facilities

#### FactionInventory.ITEMS

> `readonly` **ITEMS**: `object`

Faction item scopes

#### FactionInventory.ITEMS.ALL

> `readonly` **ALL**: `"faction_inv_items_all"` = `'faction_inv_items_all'`

Access and change anything in your faction's item inventory

#### FactionInventory.ITEMS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_items_assign"` = `'faction_inv_items_assign'`

Assign your faction's items

#### FactionInventory.ITEMS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_items_makeover"` = `'faction_inv_items_makeover'`

Make over your faction's items

#### FactionInventory.ITEMS.READ

> `readonly` **READ**: `"faction_inv_items_read"` = `'faction_inv_items_read'`

Read basic information about your faction's items

#### FactionInventory.ITEMS.RENAME

> `readonly` **RENAME**: `"faction_inv_items_rename"` = `'faction_inv_items_rename'`

Rename your faction's items

#### FactionInventory.ITEMS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_items_tags_read"` = `'faction_inv_items_tags_read'`

Read the tags assigned to your faction's items

#### FactionInventory.ITEMS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_items_tags_write"` = `'faction_inv_items_tags_write'`

Modify the tags assigned to your faction's items

#### FactionInventory.MATERIALS

> `readonly` **MATERIALS**: `object`

Faction material scopes (no ASSIGN)

#### FactionInventory.MATERIALS.ALL

> `readonly` **ALL**: `"faction_inv_materials_all"` = `'faction_inv_materials_all'`

Access and change anything in your faction's material inventory

#### FactionInventory.MATERIALS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_materials_makeover"` = `'faction_inv_materials_makeover'`

Make over your faction's materials

#### FactionInventory.MATERIALS.READ

> `readonly` **READ**: `"faction_inv_materials_read"` = `'faction_inv_materials_read'`

Read basic information about your faction's materials

#### FactionInventory.MATERIALS.RENAME

> `readonly` **RENAME**: `"faction_inv_materials_rename"` = `'faction_inv_materials_rename'`

Rename your faction's materials

#### FactionInventory.MATERIALS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_materials_tags_read"` = `'faction_inv_materials_tags_read'`

Read the tags assigned to your faction's materials

#### FactionInventory.MATERIALS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_materials_tags_write"` = `'faction_inv_materials_tags_write'`

Modify the tags assigned to your faction's materials

#### FactionInventory.NPCS

> `readonly` **NPCS**: `object`

Faction NPC scopes (has MAKEOVER but no RENAME)

#### FactionInventory.NPCS.ALL

> `readonly` **ALL**: `"faction_inv_npcs_all"` = `'faction_inv_npcs_all'`

Access and change anything in your faction's NPC inventory

#### FactionInventory.NPCS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_npcs_assign"` = `'faction_inv_npcs_assign'`

Assign your faction's NPCs

#### FactionInventory.NPCS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_npcs_makeover"` = `'faction_inv_npcs_makeover'`

Make over your faction's NPCs

#### FactionInventory.NPCS.READ

> `readonly` **READ**: `"faction_inv_npcs_read"` = `'faction_inv_npcs_read'`

Read basic information about your faction's NPCs

#### FactionInventory.NPCS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_npcs_tags_read"` = `'faction_inv_npcs_tags_read'`

Read the tags assigned to your faction's NPCs

#### FactionInventory.NPCS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_npcs_tags_write"` = `'faction_inv_npcs_tags_write'`

Modify the tags assigned to your faction's NPCs

#### FactionInventory.OVERVIEW

> `readonly` **OVERVIEW**: `"faction_inv_overview"` = `'faction_inv_overview'`

Read basic information about your faction's inventories

#### FactionInventory.PLANETS

> `readonly` **PLANETS**: `object`

Faction planet scopes (no RENAME/MAKEOVER)

#### FactionInventory.PLANETS.ALL

> `readonly` **ALL**: `"faction_inv_planets_all"` = `'faction_inv_planets_all'`

Access and change anything in your faction's planet inventory

#### FactionInventory.PLANETS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_planets_assign"` = `'faction_inv_planets_assign'`

Assign your faction's planets

#### FactionInventory.PLANETS.READ

> `readonly` **READ**: `"faction_inv_planets_read"` = `'faction_inv_planets_read'`

Read basic information about your faction's planets

#### FactionInventory.PLANETS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_planets_tags_read"` = `'faction_inv_planets_tags_read'`

Read the tags assigned to your faction's planets

#### FactionInventory.PLANETS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_planets_tags_write"` = `'faction_inv_planets_tags_write'`

Modify the tags assigned to your faction's planets

#### FactionInventory.SHIPS

> `readonly` **SHIPS**: `object`

Faction ship scopes

#### FactionInventory.SHIPS.ALL

> `readonly` **ALL**: `"faction_inv_ships_all"` = `'faction_inv_ships_all'`

Access and change anything in your faction's ship inventory

#### FactionInventory.SHIPS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_ships_assign"` = `'faction_inv_ships_assign'`

Assign your faction's ships

#### FactionInventory.SHIPS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_ships_makeover"` = `'faction_inv_ships_makeover'`

Make over your faction's ships

#### FactionInventory.SHIPS.READ

> `readonly` **READ**: `"faction_inv_ships_read"` = `'faction_inv_ships_read'`

Read basic information about your faction's ships

#### FactionInventory.SHIPS.RENAME

> `readonly` **RENAME**: `"faction_inv_ships_rename"` = `'faction_inv_ships_rename'`

Rename your faction's ships

#### FactionInventory.SHIPS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_ships_tags_read"` = `'faction_inv_ships_tags_read'`

Read the tags assigned to your faction's ships

#### FactionInventory.SHIPS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_ships_tags_write"` = `'faction_inv_ships_tags_write'`

Modify the tags assigned to your faction's ships

#### FactionInventory.STATIONS

> `readonly` **STATIONS**: `object`

Faction station scopes

#### FactionInventory.STATIONS.ALL

> `readonly` **ALL**: `"faction_inv_stations_all"` = `'faction_inv_stations_all'`

Access and change anything in your faction's space station inventory

#### FactionInventory.STATIONS.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_stations_assign"` = `'faction_inv_stations_assign'`

Assign your faction's space stations

#### FactionInventory.STATIONS.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_stations_makeover"` = `'faction_inv_stations_makeover'`

Make over your faction's space stations

#### FactionInventory.STATIONS.READ

> `readonly` **READ**: `"faction_inv_stations_read"` = `'faction_inv_stations_read'`

Read basic information about your faction's space stations

#### FactionInventory.STATIONS.RENAME

> `readonly` **RENAME**: `"faction_inv_stations_rename"` = `'faction_inv_stations_rename'`

Rename your faction's space stations

#### FactionInventory.STATIONS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_stations_tags_read"` = `'faction_inv_stations_tags_read'`

Read the tags assigned to your faction's space stations

#### FactionInventory.STATIONS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_stations_tags_write"` = `'faction_inv_stations_tags_write'`

Modify the tags assigned to your faction's space stations

#### FactionInventory.VEHICLES

> `readonly` **VEHICLES**: `object`

Faction vehicle scopes

#### FactionInventory.VEHICLES.ALL

> `readonly` **ALL**: `"faction_inv_vehicles_all"` = `'faction_inv_vehicles_all'`

Access and change anything in your faction's vehicle inventory

#### FactionInventory.VEHICLES.ASSIGN

> `readonly` **ASSIGN**: `"faction_inv_vehicles_assign"` = `'faction_inv_vehicles_assign'`

Assign your faction's vehicles

#### FactionInventory.VEHICLES.MAKEOVER

> `readonly` **MAKEOVER**: `"faction_inv_vehicles_makeover"` = `'faction_inv_vehicles_makeover'`

Make over your faction's vehicles

#### FactionInventory.VEHICLES.READ

> `readonly` **READ**: `"faction_inv_vehicles_read"` = `'faction_inv_vehicles_read'`

Read basic information about your faction's vehicles

#### FactionInventory.VEHICLES.RENAME

> `readonly` **RENAME**: `"faction_inv_vehicles_rename"` = `'faction_inv_vehicles_rename'`

Rename your faction's vehicles

#### FactionInventory.VEHICLES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"faction_inv_vehicles_tags_read"` = `'faction_inv_vehicles_tags_read'`

Read the tags assigned to your faction's vehicles

#### FactionInventory.VEHICLES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"faction_inv_vehicles_tags_write"` = `'faction_inv_vehicles_tags_write'`

Modify the tags assigned to your faction's vehicles

### Messages

> `readonly` **Messages**: `object` = `MessageScopes`

#### Messages.ALL

> `readonly` **ALL**: `"messages_all"` = `'messages_all'`

Read, send and delete messages to or from the character

#### Messages.DELETE

> `readonly` **DELETE**: `"messages_delete"` = `'messages_delete'`

Delete messages to or from the character

#### Messages.READ

> `readonly` **READ**: `"messages_read"` = `'messages_read'`

Read messages sent to or by the character

#### Messages.SEND

> `readonly` **SEND**: `"messages_send"` = `'messages_send'`

Send messages to or from the character

### PersonalInventory

> `readonly` **PersonalInventory**: `object` = `PersonalInventoryScopes`

#### PersonalInventory.CITIES

> `readonly` **CITIES**: `object`

Personal city scopes

#### PersonalInventory.CITIES.ALL

> `readonly` **ALL**: `"personal_inv_cities_all"` = `'personal_inv_cities_all'`

Access and change anything in your city inventory

#### PersonalInventory.CITIES.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_cities_assign"` = `'personal_inv_cities_assign'`

Assign your cities

#### PersonalInventory.CITIES.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_cities_makeover"` = `'personal_inv_cities_makeover'`

Make over your cities

#### PersonalInventory.CITIES.READ

> `readonly` **READ**: `"personal_inv_cities_read"` = `'personal_inv_cities_read'`

Read basic information about your cities

#### PersonalInventory.CITIES.RENAME

> `readonly` **RENAME**: `"personal_inv_cities_rename"` = `'personal_inv_cities_rename'`

Rename your cities

#### PersonalInventory.CITIES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_cities_tags_read"` = `'personal_inv_cities_tags_read'`

Read the tags assigned to your cities

#### PersonalInventory.CITIES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_cities_tags_write"` = `'personal_inv_cities_tags_write'`

Modify the tags assigned to your cities

#### PersonalInventory.CREATURES

> `readonly` **CREATURES**: `object`

Personal creature scopes

#### PersonalInventory.CREATURES.ALL

> `readonly` **ALL**: `"personal_inv_creatures_all"` = `'personal_inv_creatures_all'`

Access and change anything in your creature inventory

#### PersonalInventory.CREATURES.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_creatures_assign"` = `'personal_inv_creatures_assign'`

Assign your creatures

#### PersonalInventory.CREATURES.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_creatures_makeover"` = `'personal_inv_creatures_makeover'`

Make over your creatures

#### PersonalInventory.CREATURES.READ

> `readonly` **READ**: `"personal_inv_creatures_read"` = `'personal_inv_creatures_read'`

Read basic information about your creatures

#### PersonalInventory.CREATURES.RENAME

> `readonly` **RENAME**: `"personal_inv_creatures_rename"` = `'personal_inv_creatures_rename'`

Rename your creatures

#### PersonalInventory.CREATURES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_creatures_tags_read"` = `'personal_inv_creatures_tags_read'`

Read the tags assigned to your creatures

#### PersonalInventory.CREATURES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_creatures_tags_write"` = `'personal_inv_creatures_tags_write'`

Modify the tags assigned to your creatures

#### PersonalInventory.DROIDS

> `readonly` **DROIDS**: `object`

Personal droid scopes

#### PersonalInventory.DROIDS.ALL

> `readonly` **ALL**: `"personal_inv_droids_all"` = `'personal_inv_droids_all'`

Access and change anything in your droid inventory

#### PersonalInventory.DROIDS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_droids_assign"` = `'personal_inv_droids_assign'`

Assign your droids

#### PersonalInventory.DROIDS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_droids_makeover"` = `'personal_inv_droids_makeover'`

Make over your droids

#### PersonalInventory.DROIDS.READ

> `readonly` **READ**: `"personal_inv_droids_read"` = `'personal_inv_droids_read'`

Read basic information about your droids

#### PersonalInventory.DROIDS.RENAME

> `readonly` **RENAME**: `"personal_inv_droids_rename"` = `'personal_inv_droids_rename'`

Rename your droids

#### PersonalInventory.DROIDS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_droids_tags_read"` = `'personal_inv_droids_tags_read'`

Read the tags assigned to your droids

#### PersonalInventory.DROIDS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_droids_tags_write"` = `'personal_inv_droids_tags_write'`

Modify the tags assigned to your droids

#### PersonalInventory.FACILITIES

> `readonly` **FACILITIES**: `object`

Personal facility scopes

#### PersonalInventory.FACILITIES.ALL

> `readonly` **ALL**: `"personal_inv_facilities_all"` = `'personal_inv_facilities_all'`

Access and change anything in your facility inventory

#### PersonalInventory.FACILITIES.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_facilities_assign"` = `'personal_inv_facilities_assign'`

Assign your facilities

#### PersonalInventory.FACILITIES.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_facilities_makeover"` = `'personal_inv_facilities_makeover'`

Make over your facilities

#### PersonalInventory.FACILITIES.READ

> `readonly` **READ**: `"personal_inv_facilities_read"` = `'personal_inv_facilities_read'`

Read basic information about your facilities

#### PersonalInventory.FACILITIES.RENAME

> `readonly` **RENAME**: `"personal_inv_facilities_rename"` = `'personal_inv_facilities_rename'`

Rename your facilities

#### PersonalInventory.FACILITIES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_facilities_tags_read"` = `'personal_inv_facilities_tags_read'`

Read the tags assigned to your facilities

#### PersonalInventory.FACILITIES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_facilities_tags_write"` = `'personal_inv_facilities_tags_write'`

Modify the tags assigned to your facilities

#### PersonalInventory.ITEMS

> `readonly` **ITEMS**: `object`

Personal item scopes

#### PersonalInventory.ITEMS.ALL

> `readonly` **ALL**: `"personal_inv_items_all"` = `'personal_inv_items_all'`

Access and change anything in your item inventory

#### PersonalInventory.ITEMS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_items_assign"` = `'personal_inv_items_assign'`

Assign your items

#### PersonalInventory.ITEMS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_items_makeover"` = `'personal_inv_items_makeover'`

Make over your items

#### PersonalInventory.ITEMS.READ

> `readonly` **READ**: `"personal_inv_items_read"` = `'personal_inv_items_read'`

Read basic information about your items

#### PersonalInventory.ITEMS.RENAME

> `readonly` **RENAME**: `"personal_inv_items_rename"` = `'personal_inv_items_rename'`

Rename your items

#### PersonalInventory.ITEMS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_items_tags_read"` = `'personal_inv_items_tags_read'`

Read the tags assigned to your items

#### PersonalInventory.ITEMS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_items_tags_write"` = `'personal_inv_items_tags_write'`

Modify the tags assigned to your items

#### PersonalInventory.MATERIALS

> `readonly` **MATERIALS**: `object`

Personal material scopes (no ASSIGN)

#### PersonalInventory.MATERIALS.ALL

> `readonly` **ALL**: `"personal_inv_materials_all"` = `'personal_inv_materials_all'`

Access and change anything in your material inventory

#### PersonalInventory.MATERIALS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_materials_makeover"` = `'personal_inv_materials_makeover'`

Make over your materials

#### PersonalInventory.MATERIALS.READ

> `readonly` **READ**: `"personal_inv_materials_read"` = `'personal_inv_materials_read'`

Read basic information about your materials

#### PersonalInventory.MATERIALS.RENAME

> `readonly` **RENAME**: `"personal_inv_materials_rename"` = `'personal_inv_materials_rename'`

Rename your materials

#### PersonalInventory.MATERIALS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_materials_tags_read"` = `'personal_inv_materials_tags_read'`

Read the tags assigned to your materials

#### PersonalInventory.MATERIALS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_materials_tags_write"` = `'personal_inv_materials_tags_write'`

Modify the tags assigned to your materials

#### PersonalInventory.NPCS

> `readonly` **NPCS**: `object`

Personal NPC scopes (has MAKEOVER but no RENAME)

#### PersonalInventory.NPCS.ALL

> `readonly` **ALL**: `"personal_inv_npcs_all"` = `'personal_inv_npcs_all'`

Access and change anything in your NPC inventory

#### PersonalInventory.NPCS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_npcs_assign"` = `'personal_inv_npcs_assign'`

Assign your NPCs

#### PersonalInventory.NPCS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_npcs_makeover"` = `'personal_inv_npcs_makeover'`

Make over your NPCs

#### PersonalInventory.NPCS.READ

> `readonly` **READ**: `"personal_inv_npcs_read"` = `'personal_inv_npcs_read'`

Read basic information about your NPCs

#### PersonalInventory.NPCS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_npcs_tags_read"` = `'personal_inv_npcs_tags_read'`

Read the tags assigned to your NPCs

#### PersonalInventory.NPCS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_npcs_tags_write"` = `'personal_inv_npcs_tags_write'`

Modify the tags assigned to your NPCs

#### PersonalInventory.OVERVIEW

> `readonly` **OVERVIEW**: `"personal_inv_overview"` = `'personal_inv_overview'`

Read basic information about your inventories

#### PersonalInventory.PLANETS

> `readonly` **PLANETS**: `object`

Personal planet scopes (no RENAME/MAKEOVER)

#### PersonalInventory.PLANETS.ALL

> `readonly` **ALL**: `"personal_inv_planets_all"` = `'personal_inv_planets_all'`

Access and change anything in your planet inventory

#### PersonalInventory.PLANETS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_planets_assign"` = `'personal_inv_planets_assign'`

Assign your planets

#### PersonalInventory.PLANETS.READ

> `readonly` **READ**: `"personal_inv_planets_read"` = `'personal_inv_planets_read'`

Read basic information about your planets

#### PersonalInventory.PLANETS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_planets_tags_read"` = `'personal_inv_planets_tags_read'`

Read the tags assigned to your planets

#### PersonalInventory.PLANETS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_planets_tags_write"` = `'personal_inv_planets_tags_write'`

Modify the tags assigned to your planets

#### PersonalInventory.SHIPS

> `readonly` **SHIPS**: `object`

Personal ship scopes

#### PersonalInventory.SHIPS.ALL

> `readonly` **ALL**: `"personal_inv_ships_all"` = `'personal_inv_ships_all'`

Access and change anything in your ship inventory

#### PersonalInventory.SHIPS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_ships_assign"` = `'personal_inv_ships_assign'`

Assign your ships

#### PersonalInventory.SHIPS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_ships_makeover"` = `'personal_inv_ships_makeover'`

Make over your ships

#### PersonalInventory.SHIPS.READ

> `readonly` **READ**: `"personal_inv_ships_read"` = `'personal_inv_ships_read'`

Read basic information about your ships

#### PersonalInventory.SHIPS.RENAME

> `readonly` **RENAME**: `"personal_inv_ships_rename"` = `'personal_inv_ships_rename'`

Rename your ships

#### PersonalInventory.SHIPS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_ships_tags_read"` = `'personal_inv_ships_tags_read'`

Read the tags assigned to your ships

#### PersonalInventory.SHIPS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_ships_tags_write"` = `'personal_inv_ships_tags_write'`

Modify the tags assigned to your ships

#### PersonalInventory.STATIONS

> `readonly` **STATIONS**: `object`

Personal station scopes

#### PersonalInventory.STATIONS.ALL

> `readonly` **ALL**: `"personal_inv_stations_all"` = `'personal_inv_stations_all'`

Access and change anything in your space station inventory

#### PersonalInventory.STATIONS.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_stations_assign"` = `'personal_inv_stations_assign'`

Assign your space stations

#### PersonalInventory.STATIONS.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_stations_makeover"` = `'personal_inv_stations_makeover'`

Make over your space stations

#### PersonalInventory.STATIONS.READ

> `readonly` **READ**: `"personal_inv_stations_read"` = `'personal_inv_stations_read'`

Read basic information about your space stations

#### PersonalInventory.STATIONS.RENAME

> `readonly` **RENAME**: `"personal_inv_stations_rename"` = `'personal_inv_stations_rename'`

Rename your space stations

#### PersonalInventory.STATIONS.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_stations_tags_read"` = `'personal_inv_stations_tags_read'`

Read the tags assigned to your space stations

#### PersonalInventory.STATIONS.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_stations_tags_write"` = `'personal_inv_stations_tags_write'`

Modify the tags assigned to your space stations

#### PersonalInventory.VEHICLES

> `readonly` **VEHICLES**: `object`

Personal vehicle scopes

#### PersonalInventory.VEHICLES.ALL

> `readonly` **ALL**: `"personal_inv_vehicles_all"` = `'personal_inv_vehicles_all'`

Access and change anything in your vehicle inventory

#### PersonalInventory.VEHICLES.ASSIGN

> `readonly` **ASSIGN**: `"personal_inv_vehicles_assign"` = `'personal_inv_vehicles_assign'`

Assign your vehicles

#### PersonalInventory.VEHICLES.MAKEOVER

> `readonly` **MAKEOVER**: `"personal_inv_vehicles_makeover"` = `'personal_inv_vehicles_makeover'`

Make over your vehicles

#### PersonalInventory.VEHICLES.READ

> `readonly` **READ**: `"personal_inv_vehicles_read"` = `'personal_inv_vehicles_read'`

Read basic information about your vehicles

#### PersonalInventory.VEHICLES.RENAME

> `readonly` **RENAME**: `"personal_inv_vehicles_rename"` = `'personal_inv_vehicles_rename'`

Rename your vehicles

#### PersonalInventory.VEHICLES.TAGS\_READ

> `readonly` **TAGS\_READ**: `"personal_inv_vehicles_tags_read"` = `'personal_inv_vehicles_tags_read'`

Read the tags assigned to your vehicles

#### PersonalInventory.VEHICLES.TAGS\_WRITE

> `readonly` **TAGS\_WRITE**: `"personal_inv_vehicles_tags_write"` = `'personal_inv_vehicles_tags_write'`

Modify the tags assigned to your vehicles

# Function: getAllScopes()

> **getAllScopes**(): `AllScopes`[]

Get all available scopes (for comprehensive testing)

## Returns

`AllScopes`[]

# Function: getReadOnlyScopes()

> **getReadOnlyScopes**(): `AllScopes`[]

Get basic read-only scopes (good for read-only integrations)

## Returns

`AllScopes`[]

# Function: getMinimalScopes()

> **getMinimalScopes**(): `CharacterScope`[]

Get minimal scopes for basic character info (authentication only)

## Returns

`CharacterScope`[]
